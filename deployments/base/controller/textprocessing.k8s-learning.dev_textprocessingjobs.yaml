---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: textprocessingjobs.textprocessing.k8s-learning.dev
spec:
  group: textprocessing.k8s-learning.dev
  names:
    kind: TextProcessingJob
    listKind: TextProcessingJobList
    plural: textprocessingjobs
    singular: textprocessingjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.processingType
      name: Processing Type
      type: string
    - jsonPath: .spec.priority
      name: Priority
      type: integer
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.queueDepth
      name: Queue Depth
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TextProcessingJob is the Schema for the textprocessingjobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TextProcessingJobSpec defines the desired state of TextProcessingJob
            properties:
              delayMs:
                description: DelayMs specifies an artificial delay in milliseconds
                  for stress testing
                format: int32
                maximum: 60000
                minimum: 0
                type: integer
              parameters:
                additionalProperties:
                  type: string
                description: Parameters contains processing-specific parameters as
                  JSON
                type: object
                x-kubernetes-preserve-unknown-fields: true
              priority:
                default: 5
                description: Priority specifies the job priority (1-10, with 10 being
                  highest)
                format: int32
                maximum: 10
                minimum: 1
                type: integer
              processingType:
                description: ProcessingType specifies the type of text processing
                  to perform
                enum:
                - wordcount
                - linecount
                - uppercase
                - lowercase
                - replace
                - extract
                type: string
              replicas:
                default: 1
                description: Replicas specifies the desired number of worker pods
                  for this job type
                format: int32
                maximum: 10
                minimum: 0
                type: integer
            required:
            - processingType
            type: object
          status:
            description: TextProcessingJobStatus defines the observed state of TextProcessingJob
            properties:
              activeReplicas:
                description: ActiveReplicas shows the current number of active worker
                  replicas
                format: int32
                type: integer
              completionTime:
                description: CompletionTime is when the job finished
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message provides additional information about the job
                  status
                type: string
              phase:
                description: Phase represents the current phase of the job
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              processedJobs:
                description: ProcessedJobs shows the total number of jobs processed
                format: int32
                type: integer
              queueDepth:
                description: QueueDepth shows the current queue depth for this job
                  type
                format: int32
                type: integer
              startTime:
                description: StartTime is when the job started processing
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      scale:
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.activeReplicas
      status: {}
